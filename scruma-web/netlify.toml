[build]
  command = "npm ci && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Next.js static export НЕ СМЕ да има SPA fallback на /index.html.
# Ако хоћеш да /vesti ради као /vesti/ (trailing slash), то решавамо преко trailingSlash у next.config.js.
