[build]
  command = "npm ci && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (ако имаш client-side руте)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
